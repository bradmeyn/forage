  @if (TempData["Error"] != null) {
  <div
    class="alert alert-danger alert-dismissible fade show small mx-auto mt-3"
    role="alert"
    style="max-width: 500px"
  >
    <div class="fw-bold fs-5 d-flex align-items-center">
      <i class="bi bi-exclamation-triangle me-3 fs-2"></i> <span>@TempData["Error"]</span>
    </div>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close"></button>
  </div>
  }
  @if (TempData["Success"] != null) {
  <div
    class="alert alert-success alert-dismissible fade show small mx-auto mt-3"
    role="alert"
    style="max-width: 500px"
  >
    <div class="fw-bold fs-5 d-flex align-items-center">
      <i class="bi bi-check me-3 fs-2"></i> <span>@TempData["Success"]</span>
    </div>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close"></button>
  </div>
  }